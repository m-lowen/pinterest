<%= form_for(item, {:url => "/items", :html => {:multipart => true}}) do |f| %>
  <%= f.label :body %>:
  <%= f.text_field :body %>

  <br/>
 	<%= f.label :photo %>:
	<%= f.file_field :photo %>
<br/>
  <%= f.submit %>
<% end %> 